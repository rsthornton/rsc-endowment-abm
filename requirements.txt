# Core ABM framework
# Pinned to match local dev — mesa 3.x imports networkx at module load
mesa==3.4.1
networkx>=3.0.0

# Web server
flask>=3.0.0

# Production server
gunicorn>=21.0.0

# Data handling (mesa dependencies)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
